El comando uname -a muestra información detallada del sistema operativo, incluyendo el nombre
del sistema, la versión del kernel y la arquitectura.

El comando whoami muestra el nombre del usuario que está utilizando el sistema actualmente.

Esta información es útil para administrar servidores porque permite identificar el entorno 
de trabajo, verificar configuraciones y realizar tareas de soporte y mantenimiento.